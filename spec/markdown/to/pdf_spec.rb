# frozen_string_literal: true

RSpec.describe Markdown::To::Pdf do
  it "has a version number" do
    expect(Markdown::To::Pdf::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
